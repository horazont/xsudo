# `xsudo`

```
usage: /usr/local/bin/xsudo [-u USER] [-i] [-h] [--] COMMAND [ARGS...]

Run an X11 application as a different user.

Optional arguments:

        -u USER The user to run the command as
                (default: root)
        -i      Clean the environment
        -h      Print this help message
```
